kind: Service
apiVersion: v1
metadata:
  namespace: home-dashboard
  name: grafana
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
    - port: 80
      targetPort: 3000

---

apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: home-dashboard
  name: grafana
  labels:
    app: grafana
spec:
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          env:
            - name: TZ
              value: ${TIMEZONE}
            - name: GF_SERVER_ROOT_URL
              value: "https://${HOME_DASHBOARD_GRAFANA_DOMAIN}"
            - name: GF_SERVER_ENABLE_GZIP
              value: "true"
            - name: GF_SECURITY_DISABLE_GRAVATAR
              value: "true"
            - name: GF_SMTP_ENABLED
              value: "true"
          envFrom:
            - secretRef:
                name: grafana-admin
            - secretRef:
                name: grafana-smtp-relay
            - configMapRef:
                name: grafana-smtp-from
          volumeMounts:
            - name: config
              mountPath: /var/lib/grafana
          ports:
            - name: http
              containerPort: 3000

      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: grafana-config
