apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: guacamole
  name: guacamole-db-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
